spring:
  application:
    name: BTL-CSDL
  datasource:
    url: "jdbc:mysql://localhost:3306/BTL_CSDL"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8080
